<h1>Listing logs</h1>

<table>
  <tr>
    <th>Project</th>
    <th>Change</th>
    <th>Body</th>
  </tr>

<% @logs.each do |log| %>
  <tr>
    <td><%=h log.project_id %></td>
    <td><%=h log.change %></td>
    <td><%=h log.body %></td>
    <td><%= link_to 'Show', log %></td>
    <td><%= link_to 'Edit', edit_log_path(log) %></td>
    <td><%= link_to 'Destroy', log, :confirm => 'Are you sure?', :method => :delete %></td>
  </tr>
<% end %>
</table>

<br />

<%= link_to 'New log', new_log_path %>